# 奇怪的邻居 - 项目宪法 (Project Constitution)
> **最高效力**：本项目所有 AI（策划、程序、PM）必须无条件遵守本宪法。如有冲突，以本文件为准。
## 第一条：基本原则
1. **文档驱动开发 (DDD)**：
   - 任何涉及逻辑变更的功能开发，**必须**先由 @策划 产出 Markdown 文档。
   - 禁止 @程序 直接根据口头描述编写复杂功能。
2. **单一数据源**：
   -    - `PROJECT_STATUS.md` 是项目进度的底层数据库。
   `DASHBOARD.md` 是项目进度的可视化展示层。
   `ASSET_REGISTRY.md` 是资源路径的唯一真理。
   `DATA_DICTIONARY.md` 是配置字段的唯一词典。
   - 完成任务后，**必须**同步更新这些文件。
3. **角色隔离**：
   - 策划只写文档，不写代码。
   - 程序只写代码，不改设计。
   - PM 只管流程，不干涉细节。
## 第二条：开发流程规范
1. **立项阶段**：
   - 用户提出需求 -> PM 评估优先级 -> 呼叫 @策划。
2. **设计阶段**：
   - @策划 读取宪法、状态与数据字典 -> 编写 `docs/xxx.md` -> 呼叫 @程序。
3. **开发阶段**：
   - **简单任务**：@程序 直接实现 -> 验收。
   - **复杂任务 (分包原则)**：若任务涉及 >3 个核心文件或 UI+逻辑混合，**必须**分步执行：
     - Step 1: 定义数据结构与接口。
     - Step 2: 编写/修改 UI 脚本（需配合人工操作）。
     - Step 3: 编写核心业务逻辑。
4. **验收阶段**：
   - PM 确认功能完成 -> 更新 `PROJECT_STATUS.md` 和 `DASHBOARD.md` -> 归档。
   3. **必须** 保持 `assets/Scripts/Game/` (逻辑) 与 `assets/Scripts/UI/` (界面) 的分离。
5. **数据字典公约 (Data Dictionary Pact)**：
   - 新增 JSON 字段前，**必须**检索 `DATA_DICTIONARY.md`。
   - 禁止创建同义冗余字段（如已有 `coin` 禁止创建 `money`）。
   - 凡新增字段，**必须**在开发完成后更新字典文档。
6. **UI 盲区免责 (UI Blind Spot)**：

4. **必须** 显式引用资源路径，严禁使用魔法字符串（Magic Strings）拼接未登记的路径。
## 第三条：代码安全与免责红线
1. **禁止** 在未理解 `NeighborsDataManager` 生成逻辑的情况下修改它。
2. **禁止** 随意删除 `assets/Remote/res/Json` 下的原始配置表。
3. **必须** 保持 `assets/Scripts/Game/` (逻辑) 与 `assets/Scripts/UI/` (界面) 的分离。
4. **必须** 显式引用资源路径，严禁使用魔法字符串（Magic Strings）拼接未登记的路径。
5. **UI 盲区免责 (UI Blind Spot)**：
   - 承认 AI 无法看见 Cocos 编辑器层级面板。
   - 凡涉及 `cc.Node` 查找、坐标 (`position`)、尺寸 (`Size`) 的代码，AI **必须**输出详细的 [人工校对指南]，指导用户在编辑器中确认层级结构。
   - 若因节点路径错误 (`getChildByName`) 导致的运行时报错，不属于 AI 逻辑错误，需用户手动修正预制体层级。
## 第四条：AI 协作礼仪
1. 每个 AI 在结束工作时，必须明确指引用户下一步该找谁（输出 `@角色名` 指令）。
2. PM 必须定期（或应用户要求）维护 `DASHBOARD.md` 中的 Mermaid 图表和任务列表。
3. 遇到无法决策的冲突，必须暂停并询问用户（人类）。
### 5.1 文档组织结构
1. **功能文档分类存放**：
   - 每个功能在 `docs/` 文件夹下创建**独立子文件夹**。
   - 文件夹命名：使用功能名称，如 `桌面证件交互系统/`、`电话系统/`、`钱包系统/` 等。
   - 禁止将功能文档散落在 `docs/` 根目录，必须归类到对应功能文件夹中。
2. **文件夹内必备文档**：
   ```
   功能名称/
   ├─ README.md              # 文档目录导航（必须）
   ├─ 📘 配置指南.md         # 零基础配置教程（必须）
   ├─ 📋 快速配置清单.md     # 快速参考手册（推荐）
   ├─ 🎨 节点层级结构图.md   # 技术架构文档（推荐）
   └─ 其他补充文档...
   ```
3. **README.md 内容规范**：
   - 必须包含文档清单和简介
   - 必须说明每个文档的用途和适用场景
   - 必须提供使用建议
   - 必须链接到相关代码文件
### 5.2 文档命名规范
1. **配置指南类**：`📘 配置指南.md` 或 `📘 [功能名]-配置指南.md`
2. **快速参考类**：`📋 快速配置清单.md` 或 `📋 [功能名]-快速清单.md`
3. **架构说明类**：`🎨 节点层级结构图.md` 或 `🎨 [功能名]-架构说明.md`
4. **策划文档类**：`🕵️ [功能名]-策划案.md`
### 5.3 配置指南文档标准（针对 Cocos Creator）
程序开发完成后，**必须**输出以下三类文档：
1. **📘 配置指南.md**（详细版）：
   - 目标读者：零基础Cocos Creator用户
   - 必须包含：
     - 准备工作清单
     - 分步骤详细说明（至少6步）
     - 每个步骤配有属性表格
     - 常见问题排查（至少5个问题）
     - 测试检查清单
   - 预计阅读时间：30-45分钟
2. **📋 快速配置清单.md**（精简版）：
   - 目标读者：熟悉流程的开发者
   - 必须包含：
     - 一页纸配置要点
     - 勾选框格式（可打印）
     - 快速排错表
   - 预计阅读时间：5分钟
3. **🎨 节点层级结构图.md**（架构版）：
   - 目标读者：需要理解系统架构的开发者
   - 必须包含：
     - ASCII艺术图展示节点层级
     - 组件关联关系图
     - 交互流程图
     - 数据流向图
     - 代码集成点说明
### 5.4 文档维护规范
1. 功能开发完成后，立即创建对应文档文件夹。
2. 功能修改后，同步更新文档内容。
3. 文档版本号与功能版本号保持一致。
4. 废弃的文档移动到 `docs/archive/` 归档文件夹。
### 5.5 强制执行
- @程序 在完成功能开发后，**必须**按照上述规范创建文档文件夹和三类文档。
- @PM 在验收时，**必须**检查文档完整性，文档不全视为功能未完成。
